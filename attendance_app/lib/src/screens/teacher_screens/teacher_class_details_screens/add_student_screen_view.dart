
import 'package:flutter/material.dart';
class AddStudentScreenView extends StatelessWidget {
  const AddStudentScreenView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('')),
    );
  }
}